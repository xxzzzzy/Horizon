theme: jekyll-theme-cayman
title: Horizon Daily
description: "AI 精选每日简报 / AI-curated daily digest"
baseurl: "/Horizon" # the subpath of your site, e.g. /blog
url: "https://xxzzzzy.github.io" # the base hostname & protocol for your site, e.g. http://example.com

# Build settings
markdown: kramdown
future: true # Allow future posts to be published
plugins:
  - jekyll-feed
feed:
  path: feed.xml
exclude:
  - ".github"
  - "node_modules"

defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "default"

